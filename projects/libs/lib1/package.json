{
  "name": "@libs/lib1",
  "version": "0.0.1",
  "main": "src/public-api",
  "peerDependencies": {
    "@angular/common": "^15.2.0",
    "@angular/core": "^15.2.0"
  },
  "dependencies": {
    "tslib": "^2.3.0"
  },
  "sideEffects": false,
  "scripts": {
    "ng:build": "ng build lib1 --configuration production",
    "build": "npx turbo run ng:build",
    "ng:test": "ng test lib1 --watch false",
    "test": "npx turbo run ng:test"
  }
}